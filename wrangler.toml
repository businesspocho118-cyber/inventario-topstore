name = "inventario-topstore"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[vars]
NODE_VERSION = "18"

# Configuración de Cloudflare Pages
# El build se hace con: npm run build
# Output: dist/
